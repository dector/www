# Agent Notes

- The website in the repository root uses Bun for package management and scripts.
- Use `bun run dev`, `bun run build`, and `bun run preview` for local workflows.
- Use djot for posts.
