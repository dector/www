<article class="mt-8 flex flex-col gap-4 post-content">
    <slot />
</article>

<style>
    .post-content a {
        color: var(--color-normal-lighter);
        text-decoration: underline;
        text-decoration-thickness: 1px;
        text-underline-offset: 5px;
    }

    .post-content a:hover {
        color: var(--color-normal-lighter2);
        text-decoration-color: var(--color-link3);
        text-decoration-thickness: 2px;
        text-underline-offset: 6px;
    }

    .post-content iframe {
        display: block;
        max-width: 100%;
        margin-inline: auto;
    }

    .post-content code {
        font-family: var(--font-mono);
        font-size: 0.875rem;
    }

    .post-content code:not(pre code) {
        background: var(--color-muted);
        padding-inline: 0.25rem;
        color: var(--color-normal-lighter3);
    }

    .post-content div:not(.code-block) {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .post-content li p {
        display: inline;
    }

    .post-content .code-block {
        position: relative;
    }

    .post-content .code-block .lang-tag {
        pointer-events: none;
        position: absolute;
        top: 0;
        right: 0;
        display: inline;
        padding: 0.5rem;
        font-size: 0.875rem;
        text-transform: uppercase;
        color: var(--color-muted);
    }

    .post-content pre {
        margin: 0;
        overflow-x: auto;
        padding: 0;
        font-size: 0.92rem;
    }

    .post-content pre code.hljs {
        display: block;
        overflow-x: auto;
        background: #282b2e;
        padding: 1rem;
        line-height: 1.5rem;
        color: #e0e2e4;
    }

    .post-content .hljs-keyword,
    .post-content .hljs-literal,
    .post-content .hljs-selector-id,
    .post-content .hljs-selector-tag {
        color: #93c763;
    }

    .post-content .hljs-number {
        color: #ffcd22;
    }

    .post-content .hljs-attribute {
        color: #668bb0;
    }

    .post-content .hljs-link,
    .post-content .hljs-regexp {
        color: #d39745;
    }

    .post-content .hljs-meta {
        color: #557182;
    }

    .post-content .hljs-addition,
    .post-content .hljs-built_in,
    .post-content .hljs-bullet,
    .post-content .hljs-emphasis,
    .post-content .hljs-name,
    .post-content .hljs-selector-attr,
    .post-content .hljs-selector-pseudo,
    .post-content .hljs-subst,
    .post-content .hljs-tag,
    .post-content .hljs-template-tag,
    .post-content .hljs-template-variable,
    .post-content .hljs-type,
    .post-content .hljs-variable {
        color: #8cbbad;
    }

    .post-content .hljs-string,
    .post-content .hljs-symbol {
        color: #ec7600;
    }

    .post-content .hljs-comment,
    .post-content .hljs-deletion,
    .post-content .hljs-quote {
        color: #818e96;
    }

    .post-content .hljs-selector-class {
        color: #a082bd;
    }

    .post-content .hljs-class .hljs-title,
    .post-content .hljs-code,
    .post-content .hljs-section {
        color: white;
    }

    .post-content .hljs-doctag,
    .post-content .hljs-keyword,
    .post-content .hljs-literal,
    .post-content .hljs-name,
    .post-content .hljs-section,
    .post-content .hljs-selector-tag,
    .post-content .hljs-strong,
    .post-content .hljs-title,
    .post-content .hljs-type {
        font-weight: 700;
    }

    .post-content ul {
        list-style: disc inside;
        padding-inline-start: 1rem;
    }
</style>
